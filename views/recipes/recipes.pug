extends ../_layouts/base

block body
  include ../_includes/search.pug
  .container.container-1
    p.js-recipes-tally= filters.i.tally(data.recipes.recipes, ['recipe', 'recipes'])
    +recipes(data.recipes.recipes)

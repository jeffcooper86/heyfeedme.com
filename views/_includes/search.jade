.m-search
  .container.container-1
    .module-content-wrap
      .module-content
        .spaced-1
          form(action='/search').text-center
            input(type='text',
              name='q',
              placeholder='What do you want?'
            ).sm-search
            select(name='f').select
              each category in data.categories
                option= category
            input(type='submit',
              value='Search').btn.btn-1.sm-search
